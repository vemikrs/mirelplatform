# お知らせ機能（Announcement）設計書

## 1. 概要

システム管理者・テナント管理者からユーザーへの告知・通知を管理する機能。
メンテナンス予告、新機能リリース、重要なお知らせなどを配信し、ホーム画面で確認可能にする。

---

## 2. 鳥瞰図

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                            お知らせ機能 アーキテクチャ概要                           │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  【お知らせの種類と配信範囲】                                                        │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   ┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐        │   │
│  │   │  システム全体    │   │  テナント向け    │   │  個人向け        │        │   │
│  │   │  (SYSTEM_ADMIN)  │   │  (TENANT_OWNER)  │   │  (個別通知)      │        │   │
│  │   ├──────────────────┤   ├──────────────────┤   ├──────────────────┤        │   │
│  │   │ ・メンテナンス   │   │ ・テナント固有   │   │ ・アカウント     │        │   │
│  │   │ ・新機能リリース │   │   お知らせ       │   │   関連通知       │        │   │
│  │   │ ・障害情報       │   │ ・契約更新案内   │   │ ・タスク通知     │        │   │
│  │   │ ・利用規約変更   │   │ ・社内お知らせ   │   │ ・承認依頼       │        │   │
│  │   └──────────────────┘   └──────────────────┘   └──────────────────┘        │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  【配信フロー】                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   管理者                                                                      │   │
│  │   ┌─────────┐       ┌─────────────────┐       ┌─────────────────────────┐    │   │
│  │   │ 作成    │──────▶│ Announcement    │──────▶│ AnnouncementRecipient   │    │   │
│  │   │ (Draft) │       │ (Published)     │       │ (ユーザー毎の既読管理)   │    │   │
│  │   └─────────┘       └─────────────────┘       └─────────────────────────┘    │   │
│  │                              │                                                │   │
│  │                              ▼                                                │   │
│  │                     ┌─────────────────┐                                       │   │
│  │                     │ 通知チャネル     │                                       │   │
│  │                     │ ・ホーム画面     │                                       │   │
│  │                     │ ・ベル通知       │                                       │   │
│  │                     │ ・メール (opt)   │                                       │   │
│  │                     └─────────────────┘                                       │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  【ホーム画面表示】                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   /home                                                                       │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐    │   │
│  │   │  📢 お知らせ                                        [すべて見る]    │    │   │
│  │   ├─────────────────────────────────────────────────────────────────────┤    │   │
│  │   │  🔴 【重要】システムメンテナンスのお知らせ         2025/12/01     │    │   │
│  │   │  🟡 新機能「組織管理」をリリースしました           2025/11/28     │    │   │
│  │   │  ⚪ 利用規約を改定しました                         2025/11/25     │    │   │
│  │   └─────────────────────────────────────────────────────────────────────┘    │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. データモデル

### 3.1 ER図

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                            お知らせ機能 ER図                                         │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌────────────────────────────────────────────────────────────────────────────┐     │
│  │                        mir_announcement (お知らせ本体)                      │     │
│  ├────────────────────────────────────────────────────────────────────────────┤     │
│  │ announcement_id (PK)      VARCHAR(36)                                       │     │
│  │ tenant_id                 VARCHAR(36)       -- null=システム全体            │     │
│  │ title                     VARCHAR(200)      -- タイトル                     │     │
│  │ content                   TEXT              -- 本文(Markdown対応)           │     │
│  │ summary                   VARCHAR(500)      -- 概要（一覧表示用）           │     │
│  │ category                  VARCHAR(50)       -- MAINTENANCE,RELEASE,INFO...  │     │
│  │ priority                  VARCHAR(20)       -- CRITICAL,HIGH,NORMAL,LOW     │     │
│  │ status                    VARCHAR(20)       -- DRAFT,SCHEDULED,PUBLISHED,.. │     │
│  │ publish_at                TIMESTAMP         -- 公開日時                     │     │
│  │ expire_at                 TIMESTAMP         -- 有効期限                     │     │
│  │ is_pinned                 BOOLEAN           -- 固定表示                     │     │
│  │ requires_acknowledgment   BOOLEAN           -- 確認必須（同意系）           │     │
│  │ author_id                 VARCHAR(36)       -- 作成者                       │     │
│  │ created_at                TIMESTAMP                                         │     │
│  │ updated_at                TIMESTAMP                                         │     │
│  └────────────────────────────────┬───────────────────────────────────────────┘     │
│                                   │                                                  │
│  ┌────────────────────────────────┼───────────────────────────────────────────┐     │
│  │                                │                                            │     │
│  │                                ▼                                            │     │
│  │  ┌───────────────────────────────────────────────────────────────────┐     │     │
│  │  │            mir_announcement_target (配信対象)                      │     │     │
│  │  ├───────────────────────────────────────────────────────────────────┤     │     │
│  │  │ id (PK)                   VARCHAR(36)                              │     │     │
│  │  │ announcement_id (FK)      VARCHAR(36)                              │     │     │
│  │  │ target_type               VARCHAR(20)   -- ALL,TENANT,ROLE,USER,ORG│     │     │
│  │  │ target_id                 VARCHAR(36)   -- 対象ID（typeに応じて）  │     │     │
│  │  └───────────────────────────────────────────────────────────────────┘     │     │
│  │                                                                             │     │
│  │                                ▼                                            │     │
│  │  ┌───────────────────────────────────────────────────────────────────┐     │     │
│  │  │            mir_announcement_read (既読管理)                        │     │     │
│  │  ├───────────────────────────────────────────────────────────────────┤     │     │
│  │  │ id (PK)                   VARCHAR(36)                              │     │     │
│  │  │ announcement_id (FK)      VARCHAR(36)                              │     │     │
│  │  │ user_id (FK)              VARCHAR(36)                              │     │     │
│  │  │ read_at                   TIMESTAMP      -- 閲覧日時               │     │     │
│  │  │ acknowledged_at           TIMESTAMP      -- 確認（同意）日時       │     │     │
│  │  └───────────────────────────────────────────────────────────────────┘     │     │
│  │                                                                             │     │
│  └─────────────────────────────────────────────────────────────────────────────┘     │
│                                                                                      │
│  【カテゴリ・優先度】                                                                │
│  Category: MAINTENANCE(メンテナンス), RELEASE(リリース), INFO(お知らせ),             │
│            ALERT(障害), TERMS(規約), EVENT(イベント)                                 │
│  Priority: CRITICAL(🔴緊急), HIGH(🟠重要), NORMAL(🟡通常), LOW(⚪軽微)               │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 Entity定義

```java
@Entity
@Table(name = "mir_announcement")
public class Announcement {
    @Id
    private String announcementId;
    
    private String tenantId;        // null = システム全体向け
    
    @Column(nullable = false)
    private String title;
    
    @Column(columnDefinition = "TEXT")
    private String content;          // Markdown対応
    
    private String summary;          // 一覧表示用概要
    
    @Enumerated(EnumType.STRING)
    private AnnouncementCategory category;
    
    @Enumerated(EnumType.STRING)
    private AnnouncementPriority priority;
    
    @Enumerated(EnumType.STRING)
    private AnnouncementStatus status;
    
    private Instant publishAt;       // 公開日時（予約投稿）
    
    private Instant expireAt;        // 有効期限
    
    @Column(name = "is_pinned")
    private Boolean isPinned = false;
    
    @Column(name = "requires_acknowledgment")
    private Boolean requiresAcknowledgment = false;  // 確認必須
    
    private String authorId;
    
    private Instant createdAt;
    private Instant updatedAt;
}

public enum AnnouncementCategory {
    MAINTENANCE,  // メンテナンス
    RELEASE,      // リリース
    INFO,         // お知らせ
    ALERT,        // 障害・緊急
    TERMS,        // 規約変更
    EVENT         // イベント
}

public enum AnnouncementPriority {
    CRITICAL,     // 緊急（赤）
    HIGH,         // 重要（オレンジ）
    NORMAL,       // 通常（黄）
    LOW           // 軽微（グレー）
}

public enum AnnouncementStatus {
    DRAFT,        // 下書き
    SCHEDULED,    // 予約公開待ち
    PUBLISHED,    // 公開中
    ARCHIVED      // アーカイブ済み
}

@Entity
@Table(name = "mir_announcement_target")
public class AnnouncementTarget {
    @Id
    private String id;
    
    private String announcementId;
    
    @Enumerated(EnumType.STRING)
    private TargetType targetType;
    
    private String targetId;  // targetTypeに応じて tenant_id, role, user_id, org_id
}

public enum TargetType {
    ALL,          // 全ユーザー
    TENANT,       // 特定テナント
    ROLE,         // 特定ロール（ADMIN等）
    USER,         // 特定ユーザー
    ORGANIZATION  // 特定組織
}

@Entity
@Table(name = "mir_announcement_read",
       uniqueConstraints = @UniqueConstraint(columnNames = {"announcement_id", "user_id"}))
public class AnnouncementRead {
    @Id
    private String id;
    
    private String announcementId;
    
    private String userId;
    
    private Instant readAt;          // 閲覧日時
    
    private Instant acknowledgedAt;  // 確認（同意）日時
}
```

---

## 4. API設計

### 4.1 ユーザー向けAPI

```yaml
# 自分宛てのお知らせ一覧取得
GET /announcements
Query:
  unreadOnly: false          # 未読のみ
  category: "RELEASE"        # カテゴリフィルタ
  limit: 10
  offset: 0
Response:
  200:
    total: 25
    unreadCount: 3
    items:
      - announcementId: "ann-001"
        title: "システムメンテナンスのお知らせ"
        summary: "12/1 深夜にメンテナンスを実施します"
        category: "MAINTENANCE"
        priority: "CRITICAL"
        publishAt: "2025-11-28T10:00:00Z"
        isRead: false
        isPinned: true
        requiresAcknowledgment: false

# お知らせ詳細取得（閲覧時に自動既読）
GET /announcements/{announcementId}
Response:
  200:
    announcementId: "ann-001"
    title: "システムメンテナンスのお知らせ"
    content: "# メンテナンス内容\n\n..."  # Markdown
    category: "MAINTENANCE"
    priority: "CRITICAL"
    publishAt: "2025-11-28T10:00:00Z"
    expireAt: "2025-12-02T00:00:00Z"
    author:
      displayName: "システム管理者"
    isRead: true
    readAt: "2025-11-28T12:00:00Z"
    requiresAcknowledgment: true
    acknowledgedAt: null

# 確認（同意）
POST /announcements/{announcementId}/acknowledge
Response:
  200:
    acknowledgedAt: "2025-11-28T12:05:00Z"

# 既読にする
POST /announcements/{announcementId}/read
Response:
  204

# 未読件数取得（ベル通知用）
GET /announcements/unread-count
Response:
  200:
    count: 3
    hasCritical: true
```

### 4.2 管理者向けAPI

```yaml
# お知らせ一覧（管理用）
GET /admin/announcements
Query:
  status: "PUBLISHED"
  category: "MAINTENANCE"
  tenantId: "tenant-001"  # SYSTEM_ADMINのみ指定可
  limit: 20
  offset: 0
Response:
  200:
    total: 50
    items:
      - announcementId: "ann-001"
        title: "..."
        status: "PUBLISHED"
        publishAt: "..."
        stats:
          totalRecipients: 100
          readCount: 75
          acknowledgedCount: 60

# お知らせ作成
POST /admin/announcements
Request:
  title: "新機能リリースのお知らせ"
  content: "# 新機能について\n\n..."
  summary: "組織管理機能をリリースしました"
  category: "RELEASE"
  priority: "NORMAL"
  status: "DRAFT"
  publishAt: "2025-11-30T09:00:00Z"  # 予約公開
  expireAt: "2025-12-31T23:59:59Z"
  isPinned: false
  requiresAcknowledgment: false
  targets:
    - type: "ALL"
    # または特定対象
    # - type: "TENANT", targetId: "tenant-001"
    # - type: "ROLE", targetId: "ADMIN"

# お知らせ更新
PUT /admin/announcements/{announcementId}
Request:
  title: "..."
  status: "PUBLISHED"  # 即時公開

# お知らせ削除（論理削除）
DELETE /admin/announcements/{announcementId}

# 配信統計取得
GET /admin/announcements/{announcementId}/stats
Response:
  200:
    totalRecipients: 100
    deliveredCount: 100
    readCount: 75
    acknowledgedCount: 60
    readRate: 0.75
    acknowledgeRate: 0.60
```

---

## 5. ホーム画面統合

### 5.1 ホーム画面レイアウト

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  ホーム                                                               user@example  │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │  📢 お知らせ                                          [すべて見る]           │   │
│  ├──────────────────────────────────────────────────────────────────────────────┤   │
│  │                                                                               │   │
│  │  ┌──────────────────────────────────────────────────────────────────────┐   │   │
│  │  │ 🔴 【重要】システムメンテナンスのお知らせ                             │   │   │
│  │  │    12/1 2:00-6:00 にサービスを停止します。                            │   │   │
│  │  │    📅 2025/11/28  👁️ 未読  ⚠️ 確認が必要です                          │   │   │
│  │  └──────────────────────────────────────────────────────────────────────┘   │   │
│  │                                                                               │   │
│  │  ┌──────────────────────────────────────────────────────────────────────┐   │   │
│  │  │ 🟡 新機能「組織管理」をリリースしました                                │   │   │
│  │  │    BPM/ワークフロー連携の基盤となる組織ツリー管理機能です。            │   │   │
│  │  │    📅 2025/11/28  ✅ 既読                                              │   │   │
│  │  └──────────────────────────────────────────────────────────────────────┘   │   │
│  │                                                                               │   │
│  │  ┌──────────────────────────────────────────────────────────────────────┐   │   │
│  │  │ ⚪ 利用規約を改定しました                                              │   │   │
│  │  │    プライバシーポリシーを更新しました。                                │   │   │
│  │  │    📅 2025/11/25  ✅ 既読                                              │   │   │
│  │  └──────────────────────────────────────────────────────────────────────┘   │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  ┌────────────────────────────────┐  ┌────────────────────────────────────────┐    │
│  │  📊 最近のアクティビティ       │  │  ⭐ クイックアクセス                   │    │
│  │  ...                           │  │  ...                                   │    │
│  └────────────────────────────────┘  └────────────────────────────────────────┘    │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 Reactコンポーネント設計

```typescript
// apps/frontend-v3/src/features/home/components/AnnouncementList.tsx
interface AnnouncementListProps {
  limit?: number;
  showViewAll?: boolean;
}

export function AnnouncementList({ limit = 5, showViewAll = true }: AnnouncementListProps) {
  const { data, isLoading } = useAnnouncements({ limit });
  
  return (
    <Card>
      <CardHeader className="flex flex-row items-center justify-between">
        <CardTitle className="flex items-center gap-2">
          <Megaphone className="h-5 w-5" />
          お知らせ
        </CardTitle>
        {showViewAll && (
          <Link to="/announcements" className="text-sm text-muted-foreground">
            すべて見る
          </Link>
        )}
      </CardHeader>
      <CardContent>
        {isLoading ? (
          <Skeleton />
        ) : (
          <div className="space-y-3">
            {data?.items.map((announcement) => (
              <AnnouncementCard key={announcement.announcementId} {...announcement} />
            ))}
          </div>
        )}
      </CardContent>
    </Card>
  );
}

// 優先度に応じたアイコン
const priorityIcon = {
  CRITICAL: <AlertCircle className="h-4 w-4 text-destructive" />,
  HIGH: <AlertTriangle className="h-4 w-4 text-warning" />,
  NORMAL: <Info className="h-4 w-4 text-info" />,
  LOW: <Circle className="h-4 w-4 text-muted-foreground" />,
};
```

---

## 6. 予約公開・自動アーカイブ

### 6.1 スケジューラ設計

```java
@Service
public class AnnouncementScheduler {
    
    @Autowired
    private AnnouncementRepository announcementRepository;
    
    /**
     * 予約公開処理（毎分実行）
     */
    @Scheduled(cron = "0 * * * * *")
    public void publishScheduledAnnouncements() {
        Instant now = Instant.now();
        
        List<Announcement> scheduled = announcementRepository
            .findByStatusAndPublishAtBefore(AnnouncementStatus.SCHEDULED, now);
        
        for (Announcement ann : scheduled) {
            ann.setStatus(AnnouncementStatus.PUBLISHED);
            announcementRepository.save(ann);
            
            // 通知送信（ベル通知、メール等）
            notificationService.sendAnnouncementNotification(ann);
            
            log.info("Published announcement: {}", ann.getAnnouncementId());
        }
    }
    
    /**
     * 期限切れアーカイブ処理（毎日3時実行）
     */
    @Scheduled(cron = "0 0 3 * * *")
    public void archiveExpiredAnnouncements() {
        Instant now = Instant.now();
        
        List<Announcement> expired = announcementRepository
            .findByStatusAndExpireAtBefore(AnnouncementStatus.PUBLISHED, now);
        
        for (Announcement ann : expired) {
            ann.setStatus(AnnouncementStatus.ARCHIVED);
            announcementRepository.save(ann);
            
            log.info("Archived expired announcement: {}", ann.getAnnouncementId());
        }
    }
}
```

---

## 7. 実装タスク

```
【Phase 1: Entity・Repository (2日)】
□ Announcement Entity
□ AnnouncementTarget Entity
□ AnnouncementRead Entity
□ Repository実装

【Phase 2: ユーザー向けAPI (2日)】
□ AnnouncementController (一覧、詳細、既読、確認)
□ 配信対象フィルタリングロジック
□ 既読/未読管理

【Phase 3: 管理者向けAPI (2日)】
□ AdminAnnouncementController (CRUD)
□ 配信統計
□ 認可設定

【Phase 4: スケジューラ (1日)】
□ 予約公開
□ 自動アーカイブ

【Phase 5: ホーム画面UI (3日)】
□ AnnouncementList コンポーネント
□ AnnouncementCard コンポーネント
□ お知らせ詳細モーダル/ページ
□ /home への統合

【Phase 6: 管理UI (2日)】
□ Admin お知らせ管理画面
□ Markdownエディタ統合
□ 配信対象設定UI
```

---

## 8. 工数見積もり

| 機能 | 見積もり | 備考 |
|------|----------|------|
| Entity・Repository | 2日 | |
| ユーザー向けAPI | 2日 | 配信対象フィルタ含む |
| 管理者向けAPI | 2日 | 統計含む |
| スケジューラ | 1日 | 予約公開・アーカイブ |
| ホーム画面UI | 3日 | コンポーネント + 統合 |
| 管理UI | 2日 | Markdownエディタ |
| **合計** | **12日** | |

---

**作成日**: 2025年11月28日  
**作成者**: GitHub Copilot 🤖


