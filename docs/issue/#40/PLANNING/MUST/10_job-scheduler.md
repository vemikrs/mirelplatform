# ジョブスケジューラ機能設計書

## 1. 概要

任意のジョブを任意のタイミングで実行し、実行ログを確認できる機能。
Spring Batchをベースに拡張し、Webベースの管理・監視機能を提供する。

---

## 2. 鳥瞰図

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                          ジョブスケジューラ アーキテクチャ概要                       │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  【ジョブ実行方式】                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   ┌───────────────────┐   ┌───────────────────┐   ┌───────────────────┐      │   │
│  │   │  即時実行          │   │  スケジュール実行  │   │  イベント駆動     │      │   │
│  │   │  (On-Demand)       │   │  (Cron)           │   │  (Event-Driven)   │      │   │
│  │   ├───────────────────┤   ├───────────────────┤   ├───────────────────┤      │   │
│  │   │ Admin UIから実行   │   │ Cron式で定期実行  │   │ 特定イベント発生時 │      │   │
│  │   │ API経由で実行      │   │ 日次・週次・月次  │   │ Webhook受信時     │      │   │
│  │   └───────────────────┘   └───────────────────┘   └───────────────────┘      │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  【アーキテクチャ】                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   ┌─────────────┐     ┌──────────────────┐     ┌─────────────────────────┐   │   │
│  │   │ Admin UI    │     │ JobScheduler     │     │ Spring Batch            │   │   │
│  │   │ /admin/jobs │────▶│ Service          │────▶│ JobLauncher             │   │   │
│  │   └─────────────┘     └──────────────────┘     └───────────┬─────────────┘   │   │
│  │                               │                            │                  │   │
│  │                               ▼                            ▼                  │   │
│  │                       ┌──────────────────┐     ┌─────────────────────────┐   │   │
│  │                       │ Quartz Scheduler │     │ Job Repository          │   │   │
│  │                       │ (Cron管理)       │     │ (実行履歴)              │   │   │
│  │                       └──────────────────┘     └─────────────────────────┘   │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  【ジョブタイプ例】                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   システム系                       業務系                                     │   │
│  │   ┌───────────────────────────┐   ┌───────────────────────────┐              │   │
│  │   │ ・期限切れトークン削除    │   │ ・レポート生成            │              │   │
│  │   │ ・監査ログアーカイブ      │   │ ・データエクスポート      │              │   │
│  │   │ ・一時ファイルクリーンアップ│   │ ・外部システム連携        │              │   │
│  │   │ ・ライセンス期限チェック  │   │ ・一括メール送信          │              │   │
│  │   │ ・統計情報集計            │   │ ・データインポート        │              │   │
│  │   └───────────────────────────┘   └───────────────────────────┘              │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  【実行ログ・監視】                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐    │   │
│  │   │  ジョブ実行履歴                                           [更新]   │    │   │
│  │   ├─────────────────────────────────────────────────────────────────────┤    │   │
│  │   │  ID        │ ジョブ名          │ 開始       │ 終了       │ 状態   │    │   │
│  │   │  exec-001  │ 監査ログアーカイブ │ 03:00:00   │ 03:05:23   │ ✅完了  │    │   │
│  │   │  exec-002  │ トークン削除      │ 03:30:00   │ 03:30:45   │ ✅完了  │    │   │
│  │   │  exec-003  │ レポート生成      │ 06:00:00   │ -          │ 🔄実行中│    │   │
│  │   │  exec-004  │ データインポート  │ 10:00:00   │ 10:02:15   │ ❌失敗  │    │   │
│  │   └─────────────────────────────────────────────────────────────────────┘    │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. データモデル

### 3.1 ER図

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                          ジョブスケジューラ ER図                                     │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌────────────────────────────────────────────────────────────────────────────┐     │
│  │                        mir_job_definition (ジョブ定義)                      │     │
│  ├────────────────────────────────────────────────────────────────────────────┤     │
│  │ job_id (PK)              VARCHAR(36)                                        │     │
│  │ job_name                 VARCHAR(100)   -- 一意のジョブ名                   │     │
│  │ job_type                 VARCHAR(50)    -- BATCH, SIMPLE, SCRIPT           │     │
│  │ description              VARCHAR(500)   -- 説明                             │     │
│  │ handler_class            VARCHAR(255)   -- 実行クラス名                     │     │
│  │ default_parameters       TEXT           -- デフォルトパラメータ (JSON)      │     │
│  │ timeout_seconds          INT            -- タイムアウト                     │     │
│  │ retry_count              INT            -- リトライ回数                     │     │
│  │ retry_delay_seconds      INT            -- リトライ間隔                     │     │
│  │ is_system                BOOLEAN        -- システムジョブか                 │     │
│  │ is_enabled               BOOLEAN        -- 有効/無効                        │     │
│  │ created_at               TIMESTAMP                                          │     │
│  │ updated_at               TIMESTAMP                                          │     │
│  └────────────────────────────────────────┬───────────────────────────────────┘     │
│                                           │                                          │
│  ┌────────────────────────────────────────┼───────────────────────────────────┐     │
│  │                                        │                                    │     │
│  │  ┌─────────────────────────────────────┴──────────────────────────────┐    │     │
│  │  │              mir_job_schedule (スケジュール定義)                    │    │     │
│  │  ├────────────────────────────────────────────────────────────────────┤    │     │
│  │  │ schedule_id (PK)        VARCHAR(36)                                 │    │     │
│  │  │ job_id (FK)             VARCHAR(36)                                 │    │     │
│  │  │ schedule_name           VARCHAR(100)  -- スケジュール名              │    │     │
│  │  │ cron_expression         VARCHAR(100)  -- Cron式                      │    │     │
│  │  │ timezone                VARCHAR(50)   -- Asia/Tokyo                  │    │     │
│  │  │ parameters              TEXT          -- 上書きパラメータ (JSON)     │    │     │
│  │  │ is_enabled              BOOLEAN       -- 有効/無効                   │    │     │
│  │  │ next_run_at             TIMESTAMP     -- 次回実行予定                 │    │     │
│  │  │ last_run_at             TIMESTAMP     -- 前回実行日時                 │    │     │
│  │  │ created_at              TIMESTAMP                                    │    │     │
│  │  │ updated_at              TIMESTAMP                                    │    │     │
│  │  └────────────────────────────────────────────────────────────────────┘    │     │
│  │                                        │                                    │     │
│  │                                        ▼                                    │     │
│  │  ┌────────────────────────────────────────────────────────────────────┐    │     │
│  │  │              mir_job_execution (実行履歴)                           │    │     │
│  │  ├────────────────────────────────────────────────────────────────────┤    │     │
│  │  │ execution_id (PK)       VARCHAR(36)                                 │    │     │
│  │  │ job_id (FK)             VARCHAR(36)                                 │    │     │
│  │  │ schedule_id (FK)        VARCHAR(36)   -- NULLable (即時実行)         │    │     │
│  │  │ trigger_type            VARCHAR(20)   -- SCHEDULED, MANUAL, EVENT    │    │     │
│  │  │ status                  VARCHAR(20)   -- PENDING, RUNNING, ...       │    │     │
│  │  │ parameters              TEXT          -- 実行時パラメータ (JSON)     │    │     │
│  │  │ started_at              TIMESTAMP     -- 開始日時                    │    │     │
│  │  │ finished_at             TIMESTAMP     -- 終了日時                    │    │     │
│  │  │ duration_ms             BIGINT        -- 実行時間 (ミリ秒)           │    │     │
│  │  │ exit_code               VARCHAR(20)   -- COMPLETED, FAILED, etc.     │    │     │
│  │  │ exit_message            TEXT          -- 終了メッセージ/エラー       │    │     │
│  │  │ items_processed         INT           -- 処理件数                    │    │     │
│  │  │ items_failed            INT           -- 失敗件数                    │    │     │
│  │  │ triggered_by            VARCHAR(36)   -- 実行者ユーザーID            │    │     │
│  │  │ created_at              TIMESTAMP                                    │    │     │
│  │  └────────────────────────────────────────────────────────────────────┘    │     │
│  │                                        │                                    │     │
│  │                                        ▼                                    │     │
│  │  ┌────────────────────────────────────────────────────────────────────┐    │     │
│  │  │              mir_job_execution_log (実行ログ)                       │    │     │
│  │  ├────────────────────────────────────────────────────────────────────┤    │     │
│  │  │ log_id (PK)             VARCHAR(36)                                 │    │     │
│  │  │ execution_id (FK)       VARCHAR(36)                                 │    │     │
│  │  │ log_level               VARCHAR(10)   -- INFO, WARN, ERROR          │    │     │
│  │  │ message                 TEXT          -- ログメッセージ              │    │     │
│  │  │ step_name               VARCHAR(100)  -- ステップ名 (Batch用)        │    │     │
│  │  │ created_at              TIMESTAMP                                    │    │     │
│  │  └────────────────────────────────────────────────────────────────────┘    │     │
│  │                                                                             │     │
│  └─────────────────────────────────────────────────────────────────────────────┘     │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 Entity定義

```java
@Entity
@Table(name = "mir_job_definition")
public class JobDefinition {
    @Id
    private String jobId;
    
    @Column(nullable = false, unique = true)
    private String jobName;
    
    @Enumerated(EnumType.STRING)
    private JobType jobType;
    
    private String description;
    
    private String handlerClass;      // 実行クラスFQCN
    
    @Column(columnDefinition = "TEXT")
    private String defaultParameters; // JSON
    
    private Integer timeoutSeconds;
    private Integer retryCount;
    private Integer retryDelaySeconds;
    
    @Column(name = "is_system")
    private Boolean isSystem = false;
    
    @Column(name = "is_enabled")
    private Boolean isEnabled = true;
    
    private Instant createdAt;
    private Instant updatedAt;
}

public enum JobType {
    BATCH,      // Spring Batch Job
    SIMPLE,     // シンプルなRunnable
    SCRIPT      // スクリプト実行
}

@Entity
@Table(name = "mir_job_schedule")
public class JobSchedule {
    @Id
    private String scheduleId;
    
    private String jobId;
    
    private String scheduleName;
    
    private String cronExpression;
    
    private String timezone;
    
    @Column(columnDefinition = "TEXT")
    private String parameters;
    
    @Column(name = "is_enabled")
    private Boolean isEnabled = true;
    
    private Instant nextRunAt;
    private Instant lastRunAt;
    
    private Instant createdAt;
    private Instant updatedAt;
}

@Entity
@Table(name = "mir_job_execution")
public class JobExecution {
    @Id
    private String executionId;
    
    private String jobId;
    private String scheduleId;
    
    @Enumerated(EnumType.STRING)
    private TriggerType triggerType;
    
    @Enumerated(EnumType.STRING)
    private ExecutionStatus status;
    
    @Column(columnDefinition = "TEXT")
    private String parameters;
    
    private Instant startedAt;
    private Instant finishedAt;
    
    private Long durationMs;
    
    @Enumerated(EnumType.STRING)
    private ExitCode exitCode;
    
    @Column(columnDefinition = "TEXT")
    private String exitMessage;
    
    private Integer itemsProcessed;
    private Integer itemsFailed;
    
    private String triggeredBy;
    
    private Instant createdAt;
}

public enum TriggerType {
    SCHEDULED,  // スケジュール実行
    MANUAL,     // 手動実行
    EVENT,      // イベント駆動
    RETRY       // リトライ
}

public enum ExecutionStatus {
    PENDING,    // 待機中
    RUNNING,    // 実行中
    COMPLETED,  // 完了
    FAILED,     // 失敗
    STOPPED,    // 停止
    SKIPPED     // スキップ
}

public enum ExitCode {
    COMPLETED,      // 正常完了
    FAILED,         // 失敗
    STOPPED,        // 手動停止
    UNKNOWN         // 不明
}
```

---

## 4. ジョブ実行基盤

### 4.1 ジョブハンドラインターフェース

```java
/**
 * シンプルジョブ用インターフェース
 */
public interface JobHandler {
    
    /**
     * ジョブ実行
     * @param context 実行コンテキスト
     * @return 実行結果
     */
    JobResult execute(JobExecutionContext context);
    
    /**
     * ジョブ名取得
     */
    String getJobName();
    
    /**
     * 中断可能か
     */
    default boolean isInterruptible() {
        return false;
    }
}

@Data
public class JobExecutionContext {
    private String executionId;
    private String jobId;
    private Map<String, Object> parameters;
    private Instant startedAt;
    private String triggeredBy;
    
    // 進捗報告用
    private Consumer<JobProgress> progressCallback;
    
    // 中断フラグ
    private volatile boolean interrupted = false;
}

@Data
public class JobResult {
    private ExitCode exitCode;
    private String message;
    private int itemsProcessed;
    private int itemsFailed;
    private Map<String, Object> output;
    
    public static JobResult success() {
        return success("Completed successfully");
    }
    
    public static JobResult success(String message) {
        JobResult result = new JobResult();
        result.exitCode = ExitCode.COMPLETED;
        result.message = message;
        return result;
    }
    
    public static JobResult failure(String message) {
        JobResult result = new JobResult();
        result.exitCode = ExitCode.FAILED;
        result.message = message;
        return result;
    }
}

@Data
public class JobProgress {
    private int current;
    private int total;
    private String message;
}
```

### 4.2 ジョブ実行サービス

```java
@Service
public class JobSchedulerService {
    
    @Autowired
    private JobDefinitionRepository jobDefinitionRepo;
    
    @Autowired
    private JobExecutionRepository jobExecutionRepo;
    
    @Autowired
    private ApplicationContext applicationContext;
    
    @Autowired
    private TaskExecutor jobTaskExecutor;
    
    private final Map<String, Future<?>> runningJobs = new ConcurrentHashMap<>();
    
    /**
     * ジョブを即時実行
     */
    public JobExecution executeJob(String jobId, Map<String, Object> parameters, String triggeredBy) {
        JobDefinition job = jobDefinitionRepo.findById(jobId)
            .orElseThrow(() -> new JobNotFoundException(jobId));
        
        if (!job.getIsEnabled()) {
            throw new JobDisabledException(jobId);
        }
        
        // 実行レコード作成
        JobExecution execution = new JobExecution();
        execution.setExecutionId(UUID.randomUUID().toString());
        execution.setJobId(jobId);
        execution.setTriggerType(TriggerType.MANUAL);
        execution.setStatus(ExecutionStatus.PENDING);
        execution.setParameters(toJson(parameters));
        execution.setTriggeredBy(triggeredBy);
        execution.setCreatedAt(Instant.now());
        jobExecutionRepo.save(execution);
        
        // 非同期実行
        Future<?> future = jobTaskExecutor.submit(() -> executeInternal(execution, job, parameters));
        runningJobs.put(execution.getExecutionId(), future);
        
        return execution;
    }
    
    private void executeInternal(JobExecution execution, JobDefinition job, Map<String, Object> params) {
        try {
            // ステータス更新: RUNNING
            execution.setStatus(ExecutionStatus.RUNNING);
            execution.setStartedAt(Instant.now());
            jobExecutionRepo.save(execution);
            
            // ハンドラ取得・実行
            JobHandler handler = getHandler(job.getHandlerClass());
            
            JobExecutionContext context = new JobExecutionContext();
            context.setExecutionId(execution.getExecutionId());
            context.setJobId(job.getJobId());
            context.setParameters(params);
            context.setStartedAt(execution.getStartedAt());
            context.setProgressCallback(progress -> logProgress(execution, progress));
            
            // タイムアウト設定
            JobResult result;
            if (job.getTimeoutSeconds() != null && job.getTimeoutSeconds() > 0) {
                result = executeWithTimeout(handler, context, job.getTimeoutSeconds());
            } else {
                result = handler.execute(context);
            }
            
            // 完了処理
            completeExecution(execution, result);
            
        } catch (Exception e) {
            failExecution(execution, e);
        } finally {
            runningJobs.remove(execution.getExecutionId());
        }
    }
    
    /**
     * 実行中ジョブを停止
     */
    public void stopJob(String executionId) {
        Future<?> future = runningJobs.get(executionId);
        if (future != null) {
            future.cancel(true);
            
            JobExecution execution = jobExecutionRepo.findById(executionId).orElseThrow();
            execution.setStatus(ExecutionStatus.STOPPED);
            execution.setFinishedAt(Instant.now());
            execution.setExitCode(ExitCode.STOPPED);
            execution.setExitMessage("Stopped by user");
            jobExecutionRepo.save(execution);
        }
    }
    
    private JobHandler getHandler(String handlerClass) {
        try {
            Class<?> clazz = Class.forName(handlerClass);
            return (JobHandler) applicationContext.getBean(clazz);
        } catch (Exception e) {
            throw new JobHandlerNotFoundException(handlerClass, e);
        }
    }
}
```

### 4.3 スケジューラ連携

```java
@Configuration
@EnableScheduling
public class JobSchedulerConfig {
    
    @Autowired
    private JobScheduleRepository scheduleRepo;
    
    @Autowired
    private JobSchedulerService schedulerService;
    
    /**
     * スケジュールチェック（毎分実行）
     */
    @Scheduled(cron = "0 * * * * *")
    public void checkScheduledJobs() {
        Instant now = Instant.now();
        
        List<JobSchedule> dueSchedules = scheduleRepo
            .findByIsEnabledAndNextRunAtBefore(true, now);
        
        for (JobSchedule schedule : dueSchedules) {
            try {
                // ジョブ実行
                Map<String, Object> params = parseJson(schedule.getParameters());
                schedulerService.executeJob(
                    schedule.getJobId(),
                    params,
                    "SCHEDULER"
                );
                
                // 次回実行日時を計算
                schedule.setLastRunAt(now);
                schedule.setNextRunAt(calculateNextRun(
                    schedule.getCronExpression(),
                    schedule.getTimezone()
                ));
                scheduleRepo.save(schedule);
                
            } catch (Exception e) {
                log.error("Failed to execute scheduled job: {}", schedule.getScheduleId(), e);
            }
        }
    }
    
    private Instant calculateNextRun(String cronExpression, String timezone) {
        CronExpression cron = CronExpression.parse(cronExpression);
        ZoneId zone = ZoneId.of(timezone);
        return cron.next(ZonedDateTime.now(zone)).toInstant();
    }
}
```

---

## 5. API設計

### 5.1 ジョブ管理API

```yaml
# ジョブ定義一覧
GET /admin/jobs
Query:
  type: "BATCH"
  enabled: true
Response:
  200:
    - jobId: "job-001"
      jobName: "audit-log-archive"
      jobType: "BATCH"
      description: "監査ログのアーカイブ処理"
      isSystem: true
      isEnabled: true
      schedules:
        - scheduleId: "sched-001"
          cronExpression: "0 0 3 * * *"
          isEnabled: true
          nextRunAt: "2025-11-29T03:00:00Z"

# ジョブ定義作成
POST /admin/jobs
Request:
  jobName: "report-generation"
  jobType: "SIMPLE"
  description: "日次レポート生成"
  handlerClass: "jp.vemi.mirel.jobs.ReportGenerationHandler"
  defaultParameters:
    reportType: "DAILY"
    format: "PDF"
  timeoutSeconds: 3600
  retryCount: 3
  retryDelaySeconds: 60

# ジョブ即時実行
POST /admin/jobs/{jobId}/execute
Request:
  parameters:
    reportType: "WEEKLY"
    targetDate: "2025-11-28"
Response:
  202:
    executionId: "exec-001"
    status: "PENDING"
    startedAt: null

# スケジュール作成
POST /admin/jobs/{jobId}/schedules
Request:
  scheduleName: "日次3時実行"
  cronExpression: "0 0 3 * * *"
  timezone: "Asia/Tokyo"
  parameters:
    reportType: "DAILY"

# スケジュール有効/無効切替
PATCH /admin/jobs/{jobId}/schedules/{scheduleId}
Request:
  isEnabled: false
```

### 5.2 実行履歴API

```yaml
# 実行履歴一覧
GET /admin/jobs/executions
Query:
  jobId: "job-001"
  status: "FAILED"
  from: "2025-11-01T00:00:00Z"
  to: "2025-11-28T23:59:59Z"
  limit: 50
  offset: 0
Response:
  200:
    total: 120
    items:
      - executionId: "exec-001"
        jobName: "audit-log-archive"
        triggerType: "SCHEDULED"
        status: "COMPLETED"
        startedAt: "2025-11-28T03:00:00Z"
        finishedAt: "2025-11-28T03:05:23Z"
        durationMs: 323000
        exitCode: "COMPLETED"
        itemsProcessed: 15000
        itemsFailed: 0

# 実行詳細
GET /admin/jobs/executions/{executionId}
Response:
  200:
    executionId: "exec-001"
    job:
      jobId: "job-001"
      jobName: "audit-log-archive"
    schedule:
      scheduleId: "sched-001"
      scheduleName: "日次3時実行"
    status: "COMPLETED"
    parameters:
      retentionDays: 90
    startedAt: "2025-11-28T03:00:00Z"
    finishedAt: "2025-11-28T03:05:23Z"
    durationMs: 323000
    exitCode: "COMPLETED"
    exitMessage: "Archived 15000 records"
    itemsProcessed: 15000
    itemsFailed: 0

# 実行ログ取得
GET /admin/jobs/executions/{executionId}/logs
Query:
  level: "ERROR"
  limit: 100
Response:
  200:
    - logId: "log-001"
      logLevel: "INFO"
      message: "Starting audit log archive job"
      stepName: null
      createdAt: "2025-11-28T03:00:00.123Z"
    - logId: "log-002"
      logLevel: "INFO"
      message: "Processing batch 1/15"
      stepName: "archiveStep"
      createdAt: "2025-11-28T03:00:05.456Z"

# 実行停止
POST /admin/jobs/executions/{executionId}/stop
Response:
  200:
    status: "STOPPED"
    finishedAt: "2025-11-28T03:02:15Z"

# リトライ
POST /admin/jobs/executions/{executionId}/retry
Response:
  202:
    executionId: "exec-002"
    status: "PENDING"
```

---

## 6. 標準ジョブ一覧

```
┌────────────────────────────────────────────────────────────────────────────────────┐
│                          標準システムジョブ一覧                                      │
├────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  ジョブ名                    │ スケジュール │ 説明                                  │
│ ─────────────────────────────┼──────────────┼─────────────────────────────────────  │
│ expired-token-cleanup        │ 毎日 3:00    │ 期限切れトークン(JWT/OTP/Reset)削除   │
│ audit-log-archive            │ 毎日 3:30    │ 90日以上の監査ログをアーカイブ        │
│ temp-file-cleanup            │ 毎日 4:00    │ 一時ファイル削除（72時間経過分）      │
│ license-expiry-check         │ 毎日 9:00    │ ライセンス期限チェック・通知          │
│ session-cleanup              │ 毎時 0分     │ 無効なセッションクリーンアップ        │
│ announcement-publish         │ 毎分         │ 予約お知らせの公開処理                │
│ announcement-archive         │ 毎日 3:00    │ 期限切れお知らせのアーカイブ          │
│ lockdown-schedule-check      │ 毎分         │ 閉塞スケジュールの実行                │
│ statistics-aggregate         │ 毎日 2:00    │ 日次統計情報の集計                    │
│ database-backup              │ 毎日 1:00    │ データベースバックアップ（対応環境）  │
│                              │              │                                       │
└────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 7. UI設計

### 7.1 ジョブ管理画面

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  ジョブスケジューラ                                            [+ ジョブ作成]       │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  タブ: [ジョブ一覧] [実行履歴] [スケジュール]                                        │
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │  フィルタ: タイプ [すべて▼]  状態 [有効▼]  検索 [____________]              │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │  ジョブ名                   │ タイプ  │ 次回実行          │ 状態  │ 操作    │   │
│  ├──────────────────────────────────────────────────────────────────────────────┤   │
│  │  🔄 監査ログアーカイブ      │ BATCH   │ 2025/11/29 03:00  │ ✅有効│ [▶][⚙] │   │
│  │  🔄 トークンクリーンアップ  │ SIMPLE  │ 2025/11/29 03:00  │ ✅有効│ [▶][⚙] │   │
│  │  📊 日次レポート生成        │ BATCH   │ 2025/11/29 06:00  │ ✅有効│ [▶][⚙] │   │
│  │  📧 メール送信              │ SIMPLE  │ -                 │ ⚪手動│ [▶][⚙] │   │
│  │  🔒 ライセンス期限チェック  │ SIMPLE  │ 2025/11/29 09:00  │ ⏸無効│ [▶][⚙] │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  [▶] = 即時実行  [⚙] = 設定                                                         │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 実行履歴画面

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  実行履歴                                                              [更新]       │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  フィルタ: ジョブ [すべて▼]  状態 [すべて▼]  期間 [過去7日▼]                         │
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │  実行ID    │ ジョブ名           │ トリガー │ 開始       │ 所要時間 │ 状態   │   │
│  ├──────────────────────────────────────────────────────────────────────────────┤   │
│  │  exec-005  │ レポート生成       │ 🕐定期   │ 06:00:00   │ -        │ 🔄実行中│   │
│  │  exec-004  │ トークン削除       │ 🕐定期   │ 03:30:00   │ 45秒     │ ✅完了  │   │
│  │  exec-003  │ 監査ログアーカイブ │ 🕐定期   │ 03:00:00   │ 5分23秒  │ ✅完了  │   │
│  │  exec-002  │ メール送信         │ 👤手動   │ 昨日 15:30 │ 12秒     │ ✅完了  │   │
│  │  exec-001  │ データインポート   │ 👤手動   │ 昨日 10:00 │ 2分15秒  │ ❌失敗  │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  ── 実行詳細: exec-001 ──                                                            │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │  ジョブ: データインポート                                                     │   │
│  │  開始: 2025-11-27 10:00:00  終了: 2025-11-27 10:02:15  所要: 2分15秒          │   │
│  │  状態: ❌ FAILED                                                              │   │
│  │  処理件数: 1,523 / 2,000  失敗: 477                                           │   │
│  │                                                                               │   │
│  │  エラーメッセージ:                                                            │   │
│  │  ┌────────────────────────────────────────────────────────────────────────┐  │   │
│  │  │ java.lang.IllegalArgumentException: Invalid date format at line 1524  │  │   │
│  │  └────────────────────────────────────────────────────────────────────────┘  │   │
│  │                                                                               │   │
│  │  [ログを表示] [リトライ]                                                      │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 8. 実装タスク

```
【Phase 1: Entity・Repository (2日)】
□ JobDefinition Entity
□ JobSchedule Entity
□ JobExecution Entity
□ JobExecutionLog Entity

【Phase 2: 実行基盤 (4日)】
□ JobHandler インターフェース
□ JobSchedulerService 実装
□ TaskExecutor 設定
□ タイムアウト・リトライ処理

【Phase 3: スケジューラ連携 (2日)】
□ Cron解析・次回実行計算
□ 定期チェック処理
□ スケジュール管理

【Phase 4: API (2日)】
□ AdminJobController
□ 実行・停止・リトライ
□ 認可設定

【Phase 5: 標準ジョブ実装 (3日)】
□ ExpiredTokenCleanupHandler
□ AuditLogArchiveHandler
□ TempFileCleanupHandler
□ 他標準ジョブ

【Phase 6: UI (3日)】
□ ジョブ一覧画面
□ 実行履歴画面
□ ログビューア
□ スケジュール設定UI
```

---

## 9. 工数見積もり

| 機能 | 見積もり | 備考 |
|------|----------|------|
| Entity・Repository | 2日 | |
| 実行基盤 | 4日 | タイムアウト・リトライ含む |
| スケジューラ連携 | 2日 | Cron処理 |
| API | 2日 | |
| 標準ジョブ実装 | 3日 | 5-6ジョブ |
| UI | 3日 | 3画面 |
| **合計** | **16日** | |

---

**作成日**: 2025年11月28日  
**作成者**: GitHub Copilot 🤖


