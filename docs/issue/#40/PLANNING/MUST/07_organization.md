# 組織管理（Organization）機能設計書

## 1. 概要

BPM・ワークフロー機能の基盤となる組織階層（ツリー構造）を管理する機能。
部門・チーム・役職などの組織構造を定義し、承認フロー・権限委譲・レポートラインに活用する。

---

## 2. 鳥瞰図

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              組織管理 アーキテクチャ概要                             │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  【組織ツリー構造】                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   株式会社○○ (ROOT)                                        organizationId    │   │
│  │   ├── 経営企画本部                                                           │   │
│  │   │   ├── 経営企画部                                                         │   │
│  │   │   └── 財務部                                                             │   │
│  │   ├── 技術本部                                                               │   │
│  │   │   ├── 開発部                                                             │   │
│  │   │   │   ├── 第一開発チーム                                                 │   │
│  │   │   │   └── 第二開発チーム                                                 │   │
│  │   │   └── インフラ部                                                         │   │
│  │   └── 営業本部                                                               │   │
│  │       ├── 東日本営業部                                                       │   │
│  │       └── 西日本営業部                                                       │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  【組織とユーザーの関係】                                                            │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   User ──────┬───▶ UserOrganization (多対多) ───▶ OrganizationUnit           │   │
│  │              │                                                                │   │
│  │              │      - positionType: PRIMARY / SECONDARY / TEMPORARY          │   │
│  │              │      - jobTitle: 部長 / 課長 / 担当                            │   │
│  │              │      - startDate / endDate                                     │   │
│  │              │                                                                │   │
│  │              └───▶ 1ユーザーが複数組織に所属可能（兼務対応）                  │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
│  【BPM/Workflow連携】                                                               │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                               │   │
│  │   ワークフロー定義                                                            │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐    │   │
│  │   │ 承認ルート: 申請者 → 直属上長 → 部門長 → 本部長                     │    │   │
│  │   │                ↓         ↓         ↓        ↓                       │    │   │
│  │   │ 解決方法:  申請者所属組織から親ノードを辿り、役職で判定              │    │   │
│  │   └─────────────────────────────────────────────────────────────────────┘    │   │
│  │                                                                               │   │
│  │   組織ベースの権限委譲                                                        │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐    │   │
│  │   │ 部門長不在時 → 代理承認者を自動決定（副部門長 or 上位組織長）        │    │   │
│  │   └─────────────────────────────────────────────────────────────────────┘    │   │
│  │                                                                               │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. データモデル

### 3.1 ER図

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              組織管理 ER図                                           │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌────────────────────────┐       ┌────────────────────────┐                        │
│  │   mir_organization     │       │  mir_organization_unit │                        │
│  ├────────────────────────┤       ├────────────────────────┤                        │
│  │ organization_id (PK)   │──┐    │ unit_id (PK)           │                        │
│  │ tenant_id (FK)         │  │    │ organization_id (FK)   │◀─────────┐             │
│  │ name                   │  │    │ parent_unit_id (FK)    │──────────┤ 自己参照    │
│  │ code                   │  │    │ name                   │          │             │
│  │ description            │  │    │ code                   │          │             │
│  │ fiscal_year_start      │  └───▶│ unit_type              │◀─────────┘             │
│  │ is_active              │       │ level                  │                        │
│  │ created_at             │       │ sort_order             │                        │
│  │ updated_at             │       │ is_active              │                        │
│  └────────────────────────┘       │ effective_from         │                        │
│                                   │ effective_to           │                        │
│                                   │ created_at             │                        │
│                                   │ updated_at             │                        │
│                                   └───────────┬────────────┘                        │
│                                               │                                      │
│  ┌────────────────────────┐                   │                                      │
│  │   mir_user             │                   │                                      │
│  ├────────────────────────┤                   │                                      │
│  │ user_id (PK)           │                   │                                      │
│  └───────────┬────────────┘                   │                                      │
│              │                                │                                      │
│              │         ┌──────────────────────┴──────────────────────┐              │
│              │         │           mir_user_organization             │              │
│              │         ├─────────────────────────────────────────────┤              │
│              └────────▶│ id (PK)                                     │              │
│                        │ user_id (FK)                                │              │
│                        │ unit_id (FK)                                │◀─────────────┤
│                        │ position_type (PRIMARY/SECONDARY/TEMPORARY) │              │
│                        │ job_title                                   │              │
│                        │ job_grade                                   │              │
│                        │ is_manager (組織の長か)                     │              │
│                        │ can_approve (承認権限)                      │              │
│                        │ start_date                                  │              │
│                        │ end_date                                    │              │
│                        │ created_at                                  │              │
│                        │ updated_at                                  │              │
│                        └─────────────────────────────────────────────┘              │
│                                                                                      │
│  ┌─────────────────────────────────────────────────────────────────────────────┐    │
│  │               mir_job_title_master (役職マスタ)                              │    │
│  ├─────────────────────────────────────────────────────────────────────────────┤    │
│  │ job_title_id (PK) │ organization_id │ name │ grade │ approval_level │ ...  │    │
│  └─────────────────────────────────────────────────────────────────────────────┘    │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 Entity定義

```java
// Organization - 組織ルート（テナント配下に複数持てる設計）
@Entity
@Table(name = "mir_organization")
public class Organization {
    @Id
    private String organizationId;
    
    @Column(nullable = false)
    private String tenantId;
    
    @Column(nullable = false)
    private String name;           // 株式会社○○
    
    @Column(unique = true)
    private String code;           // CORP001
    
    private String description;
    
    @Column(name = "fiscal_year_start")
    private Integer fiscalYearStart; // 4 = 4月始まり
    
    @Column(name = "is_active")
    private Boolean isActive = true;
    
    private Instant createdAt;
    private Instant updatedAt;
}

// OrganizationUnit - 組織ノード（ツリー構造）
@Entity
@Table(name = "mir_organization_unit")
public class OrganizationUnit {
    @Id
    private String unitId;
    
    @Column(nullable = false)
    private String organizationId;
    
    @Column(name = "parent_unit_id")
    private String parentUnitId;   // null = ルートノード
    
    @Column(nullable = false)
    private String name;           // 開発部
    
    @Column(unique = true)
    private String code;           // DEV001
    
    @Enumerated(EnumType.STRING)
    private UnitType unitType;     // DIVISION, DEPARTMENT, SECTION, TEAM, PROJECT
    
    private Integer level;         // 階層レベル（ROOT=0）
    
    private Integer sortOrder;     // 同一階層内の表示順
    
    @Column(name = "is_active")
    private Boolean isActive = true;
    
    private LocalDate effectiveFrom;
    private LocalDate effectiveTo;  // 組織改編対応
    
    private Instant createdAt;
    private Instant updatedAt;
    
    // ツリー操作用
    @Transient
    private List<OrganizationUnit> children;
}

public enum UnitType {
    COMPANY,      // 会社
    DIVISION,     // 本部
    DEPARTMENT,   // 部
    SECTION,      // 課
    TEAM,         // チーム
    PROJECT,      // プロジェクト（期間限定）
    VIRTUAL       // バーチャル組織
}

// UserOrganization - ユーザー所属情報
@Entity
@Table(name = "mir_user_organization")
public class UserOrganization {
    @Id
    private String id;
    
    @Column(nullable = false)
    private String userId;
    
    @Column(nullable = false)
    private String unitId;
    
    @Enumerated(EnumType.STRING)
    private PositionType positionType;  // PRIMARY, SECONDARY, TEMPORARY
    
    private String jobTitle;       // 部長、課長、担当
    
    private Integer jobGrade;      // 職位等級（承認権限判定用）
    
    @Column(name = "is_manager")
    private Boolean isManager = false;  // この組織の長か
    
    @Column(name = "can_approve")
    private Boolean canApprove = false; // 承認権限
    
    private LocalDate startDate;
    private LocalDate endDate;
    
    private Instant createdAt;
    private Instant updatedAt;
}

public enum PositionType {
    PRIMARY,      // 主務（メイン所属）
    SECONDARY,    // 兼務
    TEMPORARY     // 出向・派遣
}
```

---

## 4. API設計

### 4.1 組織管理API（Admin向け）

```yaml
# 組織一覧取得
GET /admin/organizations
Response:
  200:
    - organizationId: "org-001"
      name: "株式会社サンプル"
      code: "SAMPLE"
      isActive: true

# 組織作成
POST /admin/organizations
Request:
  name: "株式会社サンプル"
  code: "SAMPLE"
  fiscalYearStart: 4

# 組織ツリー取得
GET /admin/organizations/{organizationId}/tree
Query:
  effectiveDate: "2025-11-28"  # 指定日時点の組織
  includeInactive: false
Response:
  200:
    root:
      unitId: "unit-root"
      name: "株式会社サンプル"
      unitType: "COMPANY"
      children:
        - unitId: "unit-001"
          name: "技術本部"
          unitType: "DIVISION"
          children:
            - unitId: "unit-002"
              name: "開発部"
              ...

# 組織ノード作成
POST /admin/organizations/{organizationId}/units
Request:
  parentUnitId: "unit-001"  # null でルート直下
  name: "新設部門"
  code: "NEW001"
  unitType: "DEPARTMENT"
  sortOrder: 10
  effectiveFrom: "2025-04-01"

# 組織ノード更新（異動含む）
PUT /admin/organizations/{organizationId}/units/{unitId}
Request:
  name: "新名称"
  parentUnitId: "unit-002"  # 親変更（組織再編）

# 組織ノード無効化
DELETE /admin/organizations/{organizationId}/units/{unitId}
Query:
  effectiveDate: "2025-03-31"  # 無効化日（論理削除）
```

### 4.2 ユーザー所属管理API

```yaml
# ユーザーの所属一覧
GET /users/{userId}/organizations
Response:
  200:
    - id: "uo-001"
      unit:
        unitId: "unit-002"
        name: "開発部"
        path: "技術本部 > 開発部"
      positionType: "PRIMARY"
      jobTitle: "課長"
      isManager: false

# ユーザー所属追加（兼務等）
POST /users/{userId}/organizations
Request:
  unitId: "unit-003"
  positionType: "SECONDARY"
  jobTitle: "PL"
  startDate: "2025-04-01"

# ユーザー所属更新
PUT /users/{userId}/organizations/{id}
Request:
  jobTitle: "部長"
  isManager: true
  canApprove: true

# ユーザー所属解除
DELETE /users/{userId}/organizations/{id}
Query:
  endDate: "2025-03-31"
```

### 4.3 組織検索API

```yaml
# 組織ノード検索
GET /organizations/search
Query:
  organizationId: "org-001"
  q: "開発"
  unitType: "DEPARTMENT"
  managerId: "user-123"  # 特定ユーザーが長の組織
Response:
  200:
    - unitId: "unit-002"
      name: "開発部"
      path: "技術本部 > 開発部"
      manager:
        userId: "user-123"
        displayName: "山田太郎"
        jobTitle: "部長"

# 上位組織取得（承認ルート用）
GET /organizations/{unitId}/ancestors
Response:
  200:
    - unitId: "unit-001"
      name: "技術本部"
      level: 1
      manager: { userId: "user-456", ... }
    - unitId: "unit-root"
      name: "株式会社サンプル"
      level: 0

# 配下メンバー取得
GET /organizations/{unitId}/members
Query:
  includeSubUnits: true  # 配下組織も含む
  positionType: "PRIMARY"
Response:
  200:
    - userId: "user-789"
      displayName: "鈴木一郎"
      jobTitle: "担当"
      unit:
        unitId: "unit-002"
        name: "開発部"
```

---

## 5. BPM/ワークフロー連携設計

### 5.1 承認ルート解決

```java
@Service
public class ApprovalRouteResolver {
    
    /**
     * 承認ルートを解決
     * @param applicantUserId 申請者
     * @param routeDefinition ルート定義（例: "DIRECT_MANAGER,DEPARTMENT_HEAD,DIVISION_HEAD"）
     */
    public List<ApprovalStep> resolveRoute(String applicantUserId, String routeDefinition) {
        // 1. 申請者の主務組織を取得
        UserOrganization primary = getPrimaryOrganization(applicantUserId);
        
        // 2. ルート定義をパース
        List<ApproverType> types = parseRouteDefinition(routeDefinition);
        
        // 3. 各ステップの承認者を解決
        List<ApprovalStep> steps = new ArrayList<>();
        OrganizationUnit currentUnit = getUnit(primary.getUnitId());
        
        for (ApproverType type : types) {
            ApprovalStep step = resolveStep(currentUnit, type);
            steps.add(step);
            
            // 次のステップ用に組織を上に辿る
            if (type.requiresParentUnit()) {
                currentUnit = getParentUnit(currentUnit);
            }
        }
        
        return steps;
    }
    
    private ApprovalStep resolveStep(OrganizationUnit unit, ApproverType type) {
        switch (type) {
            case DIRECT_MANAGER:
                return findDirectManager(unit);
            case DEPARTMENT_HEAD:
                return findHeadOfType(unit, UnitType.DEPARTMENT);
            case DIVISION_HEAD:
                return findHeadOfType(unit, UnitType.DIVISION);
            case SPECIFIC_USER:
                return resolveSpecificUser(type.getUserId());
            default:
                throw new IllegalArgumentException("Unknown approver type: " + type);
        }
    }
}

public enum ApproverType {
    DIRECT_MANAGER,     // 直属上長
    DEPARTMENT_HEAD,    // 部門長
    DIVISION_HEAD,      // 本部長
    SECTION_HEAD,       // 課長
    SPECIFIC_USER,      // 特定ユーザー
    ROLE_BASED          // ロールベース（経理部長など）
}
```

### 5.2 代理承認者解決

```java
@Service
public class DelegateResolver {
    
    /**
     * 代理承認者を解決
     */
    public Optional<User> findDelegate(String primaryApproverId, LocalDate targetDate) {
        // 1. 明示的な代理設定を確認
        Optional<Delegation> delegation = delegationRepository
            .findActiveByPrimaryUserIdAndDate(primaryApproverId, targetDate);
        
        if (delegation.isPresent()) {
            return Optional.of(delegation.get().getDelegateUser());
        }
        
        // 2. 組織階層から代理を自動決定
        UserOrganization primary = getPrimaryOrganization(primaryApproverId);
        OrganizationUnit unit = getUnit(primary.getUnitId());
        
        // 2.1 同組織の副長を探す
        Optional<User> deputy = findDeputyManager(unit);
        if (deputy.isPresent()) {
            return deputy;
        }
        
        // 2.2 上位組織の長にエスカレーション
        OrganizationUnit parent = getParentUnit(unit);
        if (parent != null) {
            return findManager(parent);
        }
        
        return Optional.empty();
    }
}
```

---

## 6. UI設計

### 6.1 組織ツリー管理画面

```
┌────────────────────────────────────────────────────────────────────────────────────┐
│  組織管理                                           [+ 新規組織] [インポート]       │
├────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  組織: [株式会社サンプル ▼]  基準日: [2025-11-28]  □非アクティブも表示             │
│                                                                                     │
│  ┌─────────────────────────────────┐  ┌──────────────────────────────────────────┐ │
│  │ 📁 株式会社サンプル             │  │  組織詳細: 開発部                         │ │
│  │  ├── 📁 経営企画本部            │  │                                           │ │
│  │  │   ├── 📄 経営企画部          │  │  コード: DEV001                           │ │
│  │  │   └── 📄 財務部              │  │  種別: 部門 (DEPARTMENT)                  │ │
│  │  ├── 📁 技術本部                │  │  階層: レベル 2                           │ │
│  │  │   ├── 📄 開発部 ←選択中      │  │  親組織: 技術本部                         │ │
│  │  │   │   ├── 📄 第一開発チーム  │  │  有効期間: 2020-04-01 〜                  │ │
│  │  │   │   └── 📄 第二開発チーム  │  │                                           │ │
│  │  │   └── 📄 インフラ部          │  │  ── 所属メンバー ──                       │ │
│  │  └── 📁 営業本部                │  │  👤 山田太郎 (部長) - 主務                 │ │
│  │      ├── 📄 東日本営業部        │  │  👤 鈴木一郎 (課長) - 主務                 │ │
│  │      └── 📄 西日本営業部        │  │  👤 佐藤花子 (担当) - 主務                 │ │
│  │                                 │  │  [+ メンバー追加]                         │ │
│  │  [+ 組織追加]                   │  │                                           │ │
│  └─────────────────────────────────┘  │  [編集] [削除] [配下を移動]               │ │
│                                       └──────────────────────────────────────────┘ │
│                                                                                     │
└────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 7. 実装タスク

```
【Phase 1: Entity・Repository (3日)】
□ Organization Entity
□ OrganizationUnit Entity（ツリー構造）
□ UserOrganization Entity
□ JobTitleMaster Entity
□ Repository実装（ツリー操作メソッド含む）

【Phase 2: 基本API (3日)】
□ OrganizationController（CRUD）
□ OrganizationUnitController（ツリー操作）
□ UserOrganizationController（所属管理）
□ 認可設定（@RequireSystemRole(ADMIN)）

【Phase 3: ツリー操作・検索 (2日)】
□ ツリー構築サービス
□ 祖先・子孫取得
□ 組織パス生成
□ メンバー一覧（配下含む）

【Phase 4: BPM連携準備 (3日)】
□ ApprovalRouteResolver
□ DelegateResolver
□ 上長取得ロジック

【Phase 5: UI (4日)】
□ Admin組織ツリー管理画面
□ ユーザー所属設定画面
□ 組織選択コンポーネント

【Phase 6: インポート/エクスポート (2日)】
□ CSVインポート
□ 一括更新（組織改編対応）
```

---

## 8. 工数見積もり

| 機能 | 見積もり | 備考 |
|------|----------|------|
| Entity・Repository | 3日 | ツリー構造 |
| 基本API | 3日 | CRUD + 認可 |
| ツリー操作・検索 | 2日 | 階層処理 |
| BPM連携準備 | 3日 | 承認ルート解決 |
| UI | 4日 | ツリーUI |
| インポート/エクスポート | 2日 | CSV対応 |
| **合計** | **17日** | |

---

**作成日**: 2025年11月28日  
**作成者**: GitHub Copilot 🤖


