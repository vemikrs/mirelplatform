# E2E Status Report (2025-11-05)

対象PR: #36 feat: refresh navigation and toast experience

## 実行サマリ
- コマンド: `pnpm -C packages/e2e i && pnpm -C packages/e2e run install:browsers && pnpm -C packages/e2e test`
- 結果: 6 passed / 51 failed / 22 skipped（約22.5分）
- レポート: `packages/e2e/playwright-report/index.html`
- 成果物: `packages/e2e/test-results/`（screenshots / videos / traces / junit / json）

## 主な失敗原因の推定
- タイトル/見出しの期待が旧UI: 期待 `ProMarker 払出画面` → 現UIは `ProMarker ワークスペース`
- セレクト操作がネイティブ前提: `selectOption`/`option`要素前提 → 現UIは design-system Select（Radix/shadcn）
- コンソールエラー1件: `500 (Internal Server Error)`（routingのconsoleチェックで検出）

## 本コミットの対策（Phase 1）
- `apps/frontend-v3/index.html` の `<title>` を `ProMarker` に変更
- routing/simple/stencil-selection のE2Eを現UIに合わせて修正
  - 見出し判定を `getByRole('heading', { name: /ProMarker/ })` に変更
  - Radix Select対応：POM（`promarker-v3.page.ts`）にクリック+`role=option`選択ロジック追加し、spec側をPOM利用へ置換

## 段階的修正プラン
1. Routing/簡易テスト/セレクト操作の全面移行（本コミット）
2. パラメータ入力/ファイルアップロード/トーストの期待を現UIに同期
3. API統合テストのモック/リトライ/タイムアウト調整
4. アクセシビリティ監査（axe）閾値/除外ルールの見直し
5. フレーク検知とワーカ/タイムアウト最適化

Powered by Copilot 🤖
