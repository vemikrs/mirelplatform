# mirel Studio Flow Designer 条件式エディタ仕様

> 型安全で直感的な条件式入力

---

## 1. 概要

mirel Studio Flow Designer の条件式エディタは、**「型安全」「モデル参照」「構文エラー防止」** を重視した UI 設計とする。

---

## 2. 条件式の役割

条件式（Condition Expression）は、フローにおける分岐判定に使用される。

**例：**
```
customer.age > 20
order.totalAmount >= 10000
OrderStatus == "APPROVED"
```

---

## 3. ユーザーが書くのは"式"のみ

mirel Studio は **式言語（Expression Language）** を採用する。

```
LeftOperand   Operator   RightOperand
```

**例：**
```
customer.age >= 18
```

---

## 4. UI の基本構成

```
┌─────────────────────────┐
│ 左：フィールド参照パレット │
│   （モデル階層をツリー表示）│
├─────────────────────────┤
│ 中央：式入力エリア        │
│   （型安全サジェスト）    │
├─────────────────────────┤
│ 右：型情報・構文チェック  │
└─────────────────────────┘
```

---

## 5. 型安全サジェスト

入力中に自動的に候補を提示する。

**例：**
```
customer.   →  customerId (string)
               age (number)
               address.zip (string)
```

**演算子候補も型ごとに制限：**

| 型 | 使える演算子 |
|----|--------------|
| string | ==, !=, like |
| number | ==, !=, >, >=, <, <= |
| date | >, >=, <, <= |
| boolean | ==, != |

---

## 6. 構文エラーチェック

入力ごとにリアルタイム解析し、以下を検出：

| エラー種別 | 説明 |
|-----------|------|
| 不明なフィールド参照 | 存在しないフィールドを参照 |
| 型違反 | number と string の比較など |
| 演算子の誤用 | 型に対応しない演算子 |
| 閉じ括弧の不足 | 構文エラー |
| AND/OR の記述ミス | 論理演算子エラー |

**例：**
```
customer.age == "abc"
→ エラー：「number と string は比較できません」
```

---

## 7. 安全性のための特別ルール

| ルール | 理由 |
|--------|------|
| Null 比較の禁止 | `== null` は不可 |
| 空文字比較の禁止 | `== ""` は禁止 |
| CodeGroup の比較 | code 値のみに限定 |

安全性重視のために採用。

---

## 8. モデル更新時の条件式再評価

- Modeler でフィールド削除・型変更があると条件式の再評価が自動実行
- 問題がある場合 Flow Designer に警告表示

---

## 9. 設計意図

- フロー分岐の誤りを減らす
- 型安全な業務ロジックの実現
- Builder の操作負荷を軽減

---

## 関連ドキュメント

- [ノード体系仕様](./node-spec.md)
- [実行モデル](./execution-model.md)
- [Modeler エラー検出](../03_modeler/error-detection.md)

---

*Powered by Copilot 🤖*
