# mirel Studio UI/UX 軌道修正設計書

> **Version**: 1.0.0  
> **Status**: Draft  
> **Created**: 2025-12-05  
> **Author**: Copilot 🤖

---

## 1. 概要

本ドキュメントは、mirel Studio の現行 UI/UX 実装と設計ドキュメント間のギャップを分析し、6観点ベースの評価に基づいて軌道修正するための設計書である。

---

## 2. 現状分析サマリ

### 2.1 現在のルート構造

```
/apps/studio/
├── (index)         → StudioHomePage（スキーマ一覧）
├── new             → StudioPage（新規フォーム）
├── :modelId        → StudioPage（フォーム編集）
├── :modelId/releases → ReleasePage
├── :modelId/data   → StudioDataListPage
├── :modelId/data/:recordId → StudioDataEditPage
└── modeler/
    ├── (index)     → ModelerHomePage
    ├── models      → ModelerModelDefinePage
    ├── records     → ModelerRecordListPage
    ├── records/:modelId/new → ModelerRecordDetailPage
    ├── records/:modelId/:recordId → ModelerRecordDetailPage
    └── codes       → ModelerCodeMasterPage
```

### 2.2 現在のレイアウト構造

| コンポーネント | 現状 |
|---------------|------|
| RootLayout | グローバルヘッダー + 左ナビ（SideNavigation）+ メイン領域 |
| ModelerLayout | 独自の3ペインレイアウト（Sidebar + Explorer + Canvas + Properties） |
| StudioPage | ツールバー + 単一キャンバス領域 |

---

## 3. GAP 分析（6観点）

### 3.1 観点①: アクター別導線（Personas & Primary Flows）

#### 現状の問題点

| 項目 | 現状 | あるべき姿 |
|------|------|-----------|
| アクターの識別 | なし | Builder / Admin / Operator の3種を識別 |
| 主要導線 | StudioHome→各機能への単純リンク | アクターごとの「最初に来るべき画面」を設定 |
| 左ナビ | `navigation.json` がほぼ空。ModelerはModelerSidebarで独自ナビ | アクター別に整理された統一ナビゲーション |

#### GAP ポイント

1. **Builder は Studio（Model/Form/Flow/Release）に集中すべき** だが、現状は `StudioHomePage` と `ModelerHomePage` が分離
2. **Admin / Operator 向けの機能**（admin/*, workflow 等）が Studio と同じナビゲーションツリーに混在
3. **ModelerSidebar** が独自ナビを持ち、RootLayout の SideNavigation と**重複・矛盾**

---

### 3.2 観点②: 情報階層（Information Architecture）の不一致

#### 現状の問題点

| 項目 | 現状 | あるべき姿 |
|------|------|-----------|
| 階層深度 | 不統一（2〜4階層） | 2〜3階層で統一 |
| 画面レイアウト | StudioPage=1カラム、ModelerLayout=3ペイン | 編集系画面は統一した3ペイン構造 |
| タイトル/セクション | 画面ごとにバラバラ | 共通ヘッダー構造 |

#### GAP ポイント

1. **StudioPage**（Form/Flow編集）と **ModelerModelDefinePage**（モデル編集）で UI パターンが異なる
2. `ModelerLayout` は 3ペイン構造を持つが、`StudioPage` は単一キャンバスのみ
3. 設計ドキュメントでは **左ナビ / 中央キャンバス / 右プロパティパネル** の統一構造を規定しているが実装されていない

---

### 3.3 観点③: Context（文脈情報）の不足

#### 現状の問題点

| 項目 | 現状 | あるべき姿 |
|------|------|-----------|
| Workspace 表示 | なし | 常時表示 |
| Draft/Release 状態 | ReleasePage でのみ確認可能 | ヘッダーに常時表示 |
| 現在の編集対象 | StudioPage のツールバーにモデル名表示のみ | パンくず + 対象モデル/フォーム/フロー名 |

#### GAP ポイント

1. **Workspace の概念が UI に未実装**（設計ドキュメントでは Workspace が中核概念）
2. **Draft 状態** が編集画面で見えない
3. **環境情報**（Dev/Stg/Prod）の表示がない

---

### 3.4 観点④: 作業フロー（User Journey）が UI に表現されていない

#### 現状の問題点

| 項目 | 現状 | あるべき姿 |
|------|------|-----------|
| 作業フロー表示 | なし | Model → Form → Flow → Release の可視化 |
| Studio Home | スキーマカード一覧のみ | 進捗概況 + Quick Actions + 作業ガイド |
| 次のアクション | なし | 「次にすべきこと」の誘導 |

#### GAP ポイント

1. **StudioHomePage** は単なるスキーマ一覧で、「何をすべきか」の導きがない
2. **M→F→F→R フロー**（Model → Form → Flow → Release）が UI に反映されていない
3. **Quick Actions** がない（設計ドキュメントでは Modeler/Create Form/Create Flow を想定）

---

### 3.5 観点⑤: 機能の重複（Redundancy）と権限領域の混在

#### 現状の問題点

| 項目 | 現状 | あるべき姿 |
|------|------|-----------|
| Builder/Operator 分離 | 混在 | 明確に分離 |
| ModelerSidebar | アプリケーション/マスタデータ/設定が混在 | Builder 向け機能に限定 |
| Admin 機能 | /admin/* として存在するが、同じ RootLayout | 別ワークベンチまたは明確な視覚的分離 |

#### GAP ポイント

1. **ModelerSidebar** に「全データ管理」があり、Data Browser（Operator 機能）と混同
2. **admin/** 配下の機能が同じナビゲーションに表示される可能性
3. **ProMarker** アプリが Studio と同階層に存在し、役割境界が曖昧

---

### 3.6 観点⑥: ドメイン境界（Studio Workbench と全体管理の分離）

#### 現状の問題点

| 項目 | 現状 | あるべき姿 |
|------|------|-----------|
| Studio 独立性 | /apps/studio 配下だが RootLayout を共有 | 独立したワークベンチ |
| ノイズ | グローバル QuickLinks や他機能が見える | Studio 専用の洗練された環境 |
| 視覚的分離 | なし | 背景色・ヘッダー変更等で明確化 |

#### GAP ポイント

1. **Studio が RootLayout に埋め込まれている**（システム全体の一部として見える）
2. **ModelerLayout** は独自だが、親の RootLayout を継承しており完全分離ではない
3. 設計ドキュメントの「Builder が作業に集中できる環境」が実現されていない

---

## 4. あるべき姿の設計

### 4.1 改善後 IA（Information Architecture）

```
mirel Studio（Builder Workbench）
│
├─ Home（Workspace Dashboard）
│     ├─ 概況: Entities / Forms / Flows / Release / Draft 数
│     ├─ Quick Actions: [Modeler] [New Form] [New Flow] [Release]
│     ├─ Recent Work: 最近の作業一覧
│     └─ Tips & Documentation
│
├─ Modeler
│     ├─ Entities（一覧 → 編集）
│     ├─ Relations（関係ビュー）
│     └─ Codes（コード体系）
│
├─ Form Designer
│     ├─ Forms（一覧 → 編集 → プレビュー）
│     └─ Layouts（共通レイアウト設定）
│
├─ Flow Designer
│     ├─ Events（イベント一覧）
│     └─ Flows（フロー編集）
│
├─ Data Browser
│     ├─ Entities（データ閲覧）
│     └─ Saved Views
│
└─ Release Center
      ├─ Drafts（下書き一覧）
      ├─ Releases（リリース履歴）
      └─ Deployments（環境別適用）
```

### 4.2 画面レイアウト統一パターン

#### Studio Editor 基本レイアウト

```
┌──────────────────────────────────────────────────────────────────┐
│ Studio Header                                                    │
│ [Logo] Workspace: xxx | Draft: v3 (unsaved) | [Search] [User]   │
├──────────────────────────────────────────────────────────────────┤
│ Context Bar                                                      │
│ Home > Modeler > Entities > Customer                             │
├────────┬─────────────────────────────────────────────┬───────────┤
│ Nav    │ Main Editor Area                            │ Property  │
│ Panel  │                                             │ Panel     │
│        │ ┌─────────────────────────────────────────┐│           │
│ [Home] │ │                                         ││ [Field]   │
│ [Model]│ │         Canvas / List / Graph           ││ [Attrs]   │
│ [Form] │ │                                         ││ [Valid]   │
│ [Flow] │ │                                         ││           │
│ [Data] │ └─────────────────────────────────────────┘│           │
│ [Rel]  │                                            │           │
├────────┴─────────────────────────────────────────────┴───────────┤
│ Status Bar: [Env: Dev] [Auto-save: 3s ago] [Errors: 0]          │
└──────────────────────────────────────────────────────────────────┘
```

#### 各画面のレイアウトパターン

| 画面 | Nav Panel | Main Area | Property Panel |
|------|-----------|-----------|----------------|
| Studio Home | 機能一覧 | Dashboard | - |
| Modeler: Entity List | 機能一覧 | Entity 一覧 | - |
| Modeler: Entity Edit | エンティティツリー | Field Editor | Field Properties |
| Form Designer | フォーム一覧 | Form Canvas | Widget Properties |
| Flow Designer | フロー一覧 | Flow Graph | Node Properties |
| Data Browser | エンティティ一覧 | Data Table | Record Details |
| Release Center | Draft/Release 切替 | Diff View | Release Info |

### 4.3 ナビゲーション構造

#### 左ナビゲーション（Studio 専用）

```tsx
const studioNavigation = [
  { id: 'home', label: 'Home', icon: 'home', path: '/apps/studio' },
  { 
    id: 'modeler', 
    label: 'Modeler', 
    icon: 'database',
    children: [
      { id: 'entities', label: 'Entities', path: '/apps/studio/modeler/entities' },
      { id: 'relations', label: 'Relations', path: '/apps/studio/modeler/relations' },
      { id: 'codes', label: 'Codes', path: '/apps/studio/modeler/codes' },
    ]
  },
  { 
    id: 'forms', 
    label: 'Form Designer', 
    icon: 'layout',
    children: [
      { id: 'form-list', label: 'Forms', path: '/apps/studio/forms' },
      { id: 'layouts', label: 'Layouts', path: '/apps/studio/layouts' },
    ]
  },
  { 
    id: 'flows', 
    label: 'Flow Designer', 
    icon: 'workflow',
    children: [
      { id: 'events', label: 'Events', path: '/apps/studio/flows/events' },
      { id: 'flow-list', label: 'Flows', path: '/apps/studio/flows' },
    ]
  },
  { id: 'data', label: 'Data Browser', icon: 'table', path: '/apps/studio/data' },
  { id: 'releases', label: 'Release Center', icon: 'rocket', path: '/apps/studio/releases' },
];
```

### 4.4 文脈情報の表示

#### Studio Header コンポーネント

```tsx
// 表示する文脈情報
interface StudioContext {
  workspace: {
    id: string;
    name: string;
  };
  draft: {
    version: number;
    status: 'saved' | 'unsaved' | 'saving';
    lastSaved?: Date;
  };
  environment: 'dev' | 'stg' | 'prod';
  currentPath: string[];  // パンくず用
}
```

---

## 5. 移行戦略

### 5.1 フェーズ分割

| Phase | 内容 | 期間目安 |
|-------|------|----------|
| Phase 1 | レイアウト統一 + 文脈表示 | 2週間 |
| Phase 2 | IA 再編成 + ルート整理 | 2週間 |
| Phase 3 | 作業フロー可視化 + Home 改善 | 1週間 |
| Phase 4 | 権限境界の明確化 | 1週間 |

### 5.2 Phase 1: レイアウト統一 + 文脈表示

**目標**: 全 Studio 画面で統一された 3 ペインレイアウトを適用

1. **StudioLayout コンポーネント作成**
   - `ModelerLayout` を拡張・汎用化
   - 左ナビ / 中央キャンバス / 右プロパティの 3 ペイン構造
   - Studio 専用ヘッダー（Workspace / Draft / Environment 表示）

2. **既存画面の移行**
   - `StudioPage` → StudioLayout 適用
   - `ModelerHomePage` → StudioLayout 適用
   - `ModelerModelDefinePage` → StudioLayout 適用

3. **Context Provider 作成**
   - `StudioContextProvider`: Workspace / Draft / Environment 管理
   - ヘッダーとパンくずへの自動連携

### 5.3 Phase 2: IA 再編成 + ルート整理

**目標**: 設計ドキュメントに沿った IA を実装

1. **ルート構造の再編成**
   ```
   /apps/studio/
   ├── (index)           → StudioHomePage（Dashboard）
   ├── modeler/
   │   ├── entities      → EntityListPage
   │   ├── entities/:id  → EntityEditPage
   │   ├── relations     → RelationViewPage
   │   └── codes         → CodeSystemPage
   ├── forms/
   │   ├── (index)       → FormListPage
   │   ├── :formId       → FormDesignerPage
   │   └── layouts       → LayoutSettingsPage
   ├── flows/
   │   ├── events        → EventListPage
   │   ├── (index)       → FlowListPage
   │   └── :flowId       → FlowDesignerPage
   ├── data/
   │   ├── (index)       → DataBrowserPage
   │   ├── :entityId     → EntityDataPage
   │   └── views         → SavedViewsPage
   └── releases/
       ├── drafts        → DraftListPage
       ├── releases      → ReleaseListPage
       └── deployments   → DeploymentPage
   ```

2. **ナビゲーション統一**
   - `ModelerSidebar` を廃止し、`StudioNavigation` に統合
   - `navigation.json` から Studio 専用ナビ設定を分離

3. **不要なルートの整理**
   - 旧 `/apps/studio/:modelId` 形式から新構造へマイグレーション

### 5.4 Phase 3: 作業フロー可視化 + Home 改善

**目標**: Builder の作業導線を明確化

1. **StudioHomePage リニューアル**
   - Workspace 概況パネル（Entity/Form/Flow/Release 数）
   - Quick Actions セクション
   - Recent Work セクション
   - Getting Started / Tips セクション

2. **作業フローウィザード**
   - 新規モデル作成時のガイダンス
   - Model → Form → Flow → Release の順序提示

3. **ステップインジケーター**
   - 現在の作業位置を視覚化

### 5.5 Phase 4: 権限境界の明確化

**目標**: Builder / Admin / Operator の分離

1. **Studio ワークベンチの独立性強化**
   - Studio 専用のヘッダー/ナビゲーション
   - 背景色・アクセントカラーの差別化

2. **アクター別エントリーポイント**
   - `/apps/studio` → Builder ワークベンチ
   - `/admin` → Admin ワークベンチ
   - `/operations` → Operator ワークベンチ（将来）

3. **権限チェックの組み込み**
   - Studio 機能への Builder 権限チェック
   - 危険操作（Delete/Release override）の制限

---

## 6. コンポーネント設計

### 6.1 新規作成コンポーネント

| コンポーネント | 役割 |
|---------------|------|
| `StudioLayout` | Studio 全画面の共通レイアウト |
| `StudioHeader` | Workspace/Draft/Environment 表示 |
| `StudioNavigation` | 左ナビゲーション（統一版） |
| `StudioContextBar` | パンくず + 現在の編集対象 |
| `StudioPropertyPanel` | 右プロパティパネル（汎用） |
| `StudioStatusBar` | 環境/保存状態/エラー表示 |
| `WorkspaceDashboard` | Home 用ダッシュボード |
| `QuickActions` | Quick Action ボタン群 |
| `RecentWorkList` | 最近の作業一覧 |
| `WorkflowStepper` | M→F→F→R フロー表示 |

### 6.2 既存コンポーネントの変更

| コンポーネント | 変更内容 |
|---------------|---------|
| `ModelerLayout` | `StudioLayout` へ統合・廃止 |
| `ModelerSidebar` | `StudioNavigation` へ統合・廃止 |
| `StudioHomePage` | Dashboard UI へリニューアル |
| `StudioPage` | StudioLayout 適用 |
| `FormDesigner` | StudioLayout の MainArea に配置 |
| `FlowDesignerContainer` | StudioLayout の MainArea に配置 |

---

## 7. 優先度と依存関係

```
[Phase 1: レイアウト統一]
    │
    ├── StudioLayout 作成 ──────────┐
    │                              │
    ├── StudioHeader 作成 ─────────┤
    │                              │
    ├── StudioContextProvider 作成 ┤
    │                              │
    └── 既存画面移行 ──────────────┘
           │
           ▼
[Phase 2: IA 再編成]
    │
    ├── ルート構造変更 ────────────┐
    │                              │
    ├── StudioNavigation 作成 ─────┤
    │                              │
    └── ModelerSidebar 廃止 ───────┘
           │
           ▼
[Phase 3: 作業フロー]
    │
    ├── WorkspaceDashboard ────────┐
    │                              │
    ├── QuickActions ──────────────┤
    │                              │
    └── WorkflowStepper ───────────┘
           │
           ▼
[Phase 4: 権限境界]
    │
    ├── アクター別エントリーポイント
    │
    └── 権限チェック組み込み
```

---

## 8. 成功指標

### 8.1 UI/UX 評価チェックリスト対応

| 観点 | 目標 |
|------|------|
| ① アクター導線 | 4項目中4項目 Yes |
| ② IA 一貫性 | 4項目中4項目 Yes |
| ③ 文脈表示 | 4項目中4項目 Yes |
| ④ 作業フロー | 4項目中4項目 Yes |
| ⑤ 権限境界 | 3項目中3項目 Yes |
| ⑥ ドメイン境界 | 3項目中3項目 Yes |

### 8.2 定量指標

- Studio 内での画面遷移回数削減（現状比 -30%）
- Builder の迷い時間削減（ユーザーテストで測定）
- コンポーネント重複の解消（ModelerSidebar 等）

---

## 9. リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| 既存 E2E テストの破損 | 高 | ルート変更時に E2E 更新を同時実施 |
| 後方互換性の喪失 | 中 | 旧ルートからのリダイレクト設定 |
| 作業量の増大 | 中 | Phase 分割による段階的移行 |
| デザイン不整合 | 低 | @mirel/ui コンポーネント活用 |

---

## 10. 関連ドキュメント

- [UI 情報アーキテクチャ](./10_cross-cutting/ui-information-architecture.md)
- [画面遷移図](./10_cross-cutting/screen-transition.md)
- [UX 原則](./01_concept/ux-principles.md)
- [Builder-Runtime 連携](./02_architecture/builder-runtime.md)

---

*Powered by Copilot 🤖*
