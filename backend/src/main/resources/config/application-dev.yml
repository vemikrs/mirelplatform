# application-dev.yml - Development Environment Configuration
spring:
  datasource:
    url: jdbc:h2:file:./data/mireldb;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1;MODE=LEGACY;LOCK_TIMEOUT=10000
    driver-class-name: org.h2.Driver
    username: sa
    password:
  session:
    jdbc:
      schema: classpath:org/springframework/session/jdbc/schema-h2.sql
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        hbm2ddl:
          halt_on_error: false
        connection:
          isolation: 2  # REPEATABLE_READ for MySQL compatibility
        jdbc:
          batch_size: 1  # Minimize batch conflicts
    show-sql: true
  h2:
    console:
      enabled: true

logging:
  level:
    jp.vemi.mirel: DEBUG
    jp.vemi.framework: DEBUG
    jp.vemi.ste: DEBUG

mirel:
  storage-dir: ./data/storage
  stencil:
    layers:
      user: ${mirel.storage-dir}/apps/promarker/stencil/user
      standard: ${mirel.storage-dir}/apps/promarker/stencil/standard
      samples: classpath:/promarker/stencil/samples
    auto-deploy-samples: true

# Development-only: Disable security for guest mode testing
# CSRF is enabled by default in production (Mipla2SecurityProperties.csrfEnabled=true)
mipla2:
  security:
    enabled: false  # ゲストモード：認証不要
    api:
      csrf-enabled: false