package ${packageGroup}.${applicationId}.app.request;

import lombok.Data;

/**
 * ${serviceName} ${eventName} リクエスト.
 *
 * @author ${author}
 * @version ${version}
 */
@Data
public class ${serviceId.ucc()}${eventId.ucc()}Request {

  // TODO 実装
}
