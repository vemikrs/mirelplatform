package ${packageGroup}.${applicationId}.domain.mapper;

import org.apache.ibatis.annotations.Mapper;

/**
 * ${serviceName}マッパー.
 *
 * @author ${author}
 * @version ${version}
 */
@Mapper
public interface ${serviceId.ucc()}Mapper {

  // TODO 実装
}
