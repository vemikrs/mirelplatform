package ${packageGroup}.${applicationId}.domain.model;

import lombok.Data;

/**
 * ${serviceName} ${eventName}パラメータモデル.
 *
 * @author ${author}
 * @version ${version}
 */
@Data
public class ${serviceId.ucc()}${eventId.ucc()}ParamModel {

  // TODO 実装
}
